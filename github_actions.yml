name: OSCP Toolkit CI/CD
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Run XSS scanner
        run: xss-scan

      - name: Run SQLi scanner
        run: sqli-scan

